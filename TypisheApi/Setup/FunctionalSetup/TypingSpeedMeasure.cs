namespace TypisheApi.Setup
{
    public enum TypingSpeedMeasure
    {
        WordsPerMinute,                // = characters per minute / 5
        CharactersPerMinute,
        CharactersPerSecond,
        OsuBumpsPerMinute              // = characters per minute / 4
    }
}
